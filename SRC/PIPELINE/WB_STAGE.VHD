LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY WB_STAGE IS
    PORT(
        CLOCK : IN STD_LOGIC;
        RESET : IN STD_LOGIC;
        DATA_WB : IN STD_LOGIC_VECTOR(37 DOWNTO 0);
        REGWRITE : OUT STD_LOGIC;
        DATA_WB_ID : OUT DATA_T
    );
END WB_STAGE;

ARCHITECTURE COMPORTAMENTO OF WB_STAGE IS
    SIGNAL MUX_MEM_2_REG : STD_LOGIC;
    SIGNAL MEMORY_RESULT : STD_LOGIC_VECTOR(INST_SIZE-1 downto 0);
    SIGNAL ULA_RESULT : STD_LOGIC_VECTOR(INST_SIZE-1 downto 0);
    SIGNAL RESULT : STD_LOGIC_VECTOR(INST_SIZE-1 downto 0);
    --TODO://DESCONCATENAR DATA
    --TODO://ATRIBUIR SIGNALS PARA INTRUCAO
BEGIN
    PROCESS(CLOCK, RESET)
    BEGIN
    IF MUX_MEM_2_REG = '0' THEN
        --MEMORIA
    ELSE
        --ALU_OUT
    END IF;

    END PROCESS;
    --TODO://DESCONCATENAR REGWRITE
END COMPORTAMENTO;